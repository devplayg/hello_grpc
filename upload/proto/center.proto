syntax = "proto3";

package upload;

service DataCenter {
    rpc Upload (stream Packet) returns (UploadResult) {
    }
}

message Packet {
    bytes data = 1;
}

message UploadResult {
    string checksum = 1;
    int64 size = 2;
}
